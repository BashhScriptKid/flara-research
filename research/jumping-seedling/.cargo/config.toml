# Target the host's vector ISA. The Ryzen 5 5500U (Zen 2) has AVX2 + FMA + F16C
# but no AVX-512; enabling these lets the compiler auto-vectorize the hot loops
# (GEMMs, contractions) with 256-bit ops instead of the baseline SSE2.
[build]
rustflags = ["-C", "target-feature=+avx2,+fma,+f16c"]
