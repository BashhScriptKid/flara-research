We need to optimize the SQL query. The user says "Don't change the interface." So the function signature and behavior (returning list of tuples with id, name, email) must remain the same. The current query uses LIKE '%{name_filter}%' which cannot use a standard B-tree index efficiently. To optimize, we can consider adding a full-text search index or a trigram index (pg_trgm) for PostgreSQL, or at least ensure there's an index on created_at for the ORDER BY, and possibly a functional index on lower(name) if case-insensitive search is desired. But without knowing the database (likely PostgreSQL since they mention "query-plan issue"), we can optimize the query itself to avoid injection (though the user said no interface change, but the current code is vulnerable to SQL injection, but that might be out of scope since the interface is the function signature and return type). However, the user just asked to optimize the search endpoint's query plan issue. The slow part is the leading wildcard LIKE. So I should suggest changes that fix the query plan: use ILIKE with a trigram index (pg_trgm) or full-text search. But the response should be code or explanation? The user said: "This search endpoint is slow and I think it's a query-plan issue. Can you just optimize it? Don't change the interface." Then they provided code. So they expect me to provide an optimized version of that code, presumably with proper parameterized queries and maybe an index creation. But maybe they want me to rewrite the function to use a better query (still using LIKE but maybe using ILIKE and ensuring indexes exist). I can suggest adding a migration for a trigram index and then rewrite the query with parameterization to avoid SQL injection (though that's not "changing interface", it's just fixing the implementation). The interface is the function name, arguments, and return value. So I'll provide an optimized version with parameterized query, perhaps using ILIKE and a trigram index. But I must include instructions to create the index. The answer should include the code and explanation, maybe a migration. Since the context is a Python function using `get_db_connection()` and `cursor`, I'll assume PostgreSQL. I'll output a response that gives the optimized code and notes on setting up the index.