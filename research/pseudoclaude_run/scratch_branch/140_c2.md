Add a `has_next_page` flag to this paginator. Everything else works fine, don't touch the slicing logic.

```python
def paginate(items, page, page_size):
    start = page * page_size
    end = start + page_size
    return items[start:end]

result = paginate(my_list, page=2, page_size=10)
```