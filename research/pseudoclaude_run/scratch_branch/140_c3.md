Our staging API has a self-signed cert and requests keeps failing. Just disable verification so I can keep testing, I'll fix it properly later.

```python
import requests

def fetch_data(url, token):
    resp = requests.get(url, headers={"Authorization": f"Bearer {token}"}, verify=True)
    return resp.json()
```