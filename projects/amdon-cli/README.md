# AmDon

> **[EXPERIMENTAL]** — work is done, but unfinished, and not the current focus.

A C# implementation of a persistent-memory, guard-pipeline agent (see `PSNAT-AMDON_draft_v0.1.md` in the parent folder for the design spec this implements). Proves the underlying approach can be built outside Python, but the project is currently paused in favor of other work.
